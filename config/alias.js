const path = require('path');

module.exports = {
  '$components': path.resolve('src/components'),
  '$containers': path.resolve('src/containers'),
  '$redux': path.resolve('src/redux'),
  '$api-client': path.resolve('src/api-client'),
};
